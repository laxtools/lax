#pragma once 

namespace lax
{
namespace channel
{

/// a simple message dispatcher
/** 
 * 간단하게 하나의 클래스에서 사용하는 용도. 
 * - 람다 함수로 등록.
 * - 클래스와 수명을 같이 함.
 */
class dispatcher
{
public: 
	// subscribe

	// post
};

} // channel
} // lax